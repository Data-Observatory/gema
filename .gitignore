# Environment
.env
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
# visor.spec's build-time vendoring of the obscura binary (fetch_obscura.py) --
# downloaded+verified per platform, never committed.
/vendor/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Output
output/
reports/
result.json

# do_catalog full-scale corpus (~100 files pulled from s3://do-catalog-resources) —
# regenerable via scripts/sample_corpus.py + manifest + AWS profile catalogo-admin.
# The pilot subset + the small ORCID slice are committed under tests/fixtures/
# instead (small, and needed for reproducibility without AWS access).
data/do_catalog/

# OS
.DS_Store
Thumbs.db

# uv
.uv/

# Test/coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Cache
.cache/

# Sisyphus internal state (not for distribution)
.sisyphus/

# DeepEval local telemetry/state (not for distribution)
.deepeval/
.omo/
.nicegui

# Student onboarding doc — plain local file, handed directly, never committed
ONBOARDING_BIBLIOTECARIA.md
